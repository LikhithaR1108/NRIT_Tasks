# Day 4 – Movie Picks Gallery (React + Vite)
Features: useState (rating, ❤️ like, review, show more), onClick/onChange, controlled inputs, conditional rendering.

## Run locally
cd Day4
npm install
npm run dev